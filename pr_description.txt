- Этап плана: PR.
- Что сделано: Добавлена функция `is_audit_task` в `.github/scripts/review-autonomous-pr-quality.py`, которая проверяет наличие слова 'audit' в `task_id` или `title`. Задачи-аудиты исключены из строгих проверок, требующих изменения runtime или script файлов (когда `operational` = True). Статус задачи `automation-quality-loop-pr-520-e3a07e26` в `agent_tasks.json` обновлён на `done`.
- Что дальше: Завершение текущей сессии.
- Зачем: Это устраняет проблему, когда PR попадал в бесконечный recovery loop из-за того, что audit задача рассматривалась как `operational` и отклонялась, если менялись только тесты или манифест.
- Почему так: Это наименьшее безопасное изменение, которое решает проблему остановки quality loop для PR #520, не нарушая проверки для других `operational` задач.
- Проверки/риски: Выполнены `go test`, `go vet`, прогнан юнит-тест `python3 .github/scripts/review-autonomous-pr-quality-test.py` (22 теста прошли успешно) и валидация `validate_agent_tasks.py`. Риск минимален, так как изменение изолировано в скрипте качества PR.

<!-- AUTONOMOUS_TASK_EVIDENCE
task_id: automation-quality-loop-pr-520-e3a07e26
status: done
acceptance:
- Root cause for stopped autonomous PR #520 is identified from PR comments, quality gate output, recovery-router ledger evidence, or CI artifacts. -> .github/scripts/review-autonomous-pr-quality.py
- The automation or manifest state is improved so the same deterministic recovery loop is not repeated, or this diagnostic task is blocked with a concrete missing-evidence reason. -> .github/scripts/review-autonomous-pr-quality.py
- If code changes are made, targeted script/workflow tests are run; no secrets, raw transcripts, production URLs, or destructive actions are introduced. -> agent_tasks.json
evidence_files:
- .github/scripts/review-autonomous-pr-quality.py
- agent_tasks.json
checks:
- python3 .github/scripts/review-autonomous-pr-quality-test.py
- python3 scripts/validate_agent_tasks.py agent_tasks.json
micro_pr_justification: Эта PR решает конкретную проблему (false positive quality loop для audit-задач), удовлетворяя всем критериям приёмки одной задачи, и содержит все необходимые тесты и изменения манифеста.
-->
